name: Build WinForms EXE

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore MatrimonialApp.csproj

      - name: Publish (single-file EXE)
        run: dotnet publish MatrimonialApp.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MatrimonialApp-win-x64
          path: bin/Release/net8.0-windows/win-x64/publish/*
